<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <link rel="stylesheet" type="text/css" href="style/index.css"/>
    <script>
        function saveToCookie() {
            var loginValue = document.getElementById("login").value;
            var passwordValue = document.getElementById("password").value;

            // Зберегти дані в куки
            document.cookie = "login=" + loginValue + "; path=/";
            document.cookie = "password=" + passwordValue + "; path=/";
            alert("Your login and password have been saved in cookies.");
        }
    </script>
</head>
<body>
<div class="login">
    <h1>Login</h1>
    <form method="post" action="/auth">
        <input type="text" name="login" placeholder="Username" required="required" id="login">
        <input type="password" name="password" placeholder="Password" required="required" id="password">
        <button type="submit" onclick="saveToCookie()" class="btn btn-primary btn-block btn-large">Let me in.</button>
    </form>

</div>
</body>
</html>